{
  "name": "pg-wrapper",
  "private": true,
  "main": "lib/index.ts",
  "scripts": {
    "test": "jest --config=../jest.unit.config.json",
    "integration": "jest --config=../jest.integration.config.json"
  },
  "type": "module",
  "devDependencies": {
    "aws-wrapper-common-lib": "../common"
  },
  "files": [
    "lib/*"
  ]
}
